# 2026-05-03

## Question

在 AI Native Chat 产品中，当模型返回的不是纯文本，而是 Function Call / Tool Call（如 `tool_calls`），前端如何处理整个流程？从接收 `tool_call` → 执行函数 → 将结果回传模型 → 渲染最终回答，描述完整前端流程和 UI 交互设计。

## Why this matters

Function Calling 是 AI Native 应用的核心交互模式之一，考察候选人对 LLM 请求-响应循环和消息数组状态管理的理解。

## Expected points

- 理解 tool_call 在流式场景下是逐 delta 到达，需按 index 拼合
- 掌握 messages[] 的完整演进链路：user → assistant(tool_call) → tool(result) → assistant(text)
- 理解 content 和 tool_calls 不会同时返回
- UI 状态机设计：区分 streaming_text 和 tool_calling 态
- 异常处理：工具执行失败时将错误回传模型
- 并行调用与循环意识

## AI Explanation

用户未作答，由 AI 直接讲解。讲解覆盖：接收流式 tool_call delta → 拼合 → 执行工具 → 追加 tool(result) 到 messages[] → 重新请求模型 → 渲染最终文本的完整链路。

## 深度追问

1. **触发 tool call 的条件是什么？** — 必须在请求中传 `tools` 参数；`tool_choice: "auto"` 下模型自主判断（数据超出知识边界、有匹配能力的工具）；`"none"` 禁止调用；`"required"` 强制调用。
2. **模型返回的是纯工具调用，还是会穿插内容？** — content 和 tool_calls 互斥，不会同一轮同时返回。看似"边说边调"是多轮交替的结果。
3. **这道题的真正考点是什么？** — 不是 API 对接熟练度，而是：是否理解流式 delta 拼合、messages[] 是唯一真相源、以及工具调用是一个有状态的多轮消息循环系统。

## 用户反馈

用户指出原题偏向实现细节（「换个合格的前端看一遍文档就能做」），认为考察 tool call **机制本身**（触发条件、content/tool_call 互斥、消息数组演进）是更有区分度的问题。

## Decision

✅ Promote to curated — 追问中关于 tool call 机制的讨论（触发条件、content 与 tool_call 互斥、messages 数组演进链路）有长期复用价值，沉淀为机制类参考题。原题的实现细节部分不沉淀。
